export const ModalCrearPublicacion = ({ isOpen, onClose, onPublicacionCreada}:any) => {

    isOpen = '';
    onClose = ''
    onPublicacionCreada = ''

    return (
        <div>
            Modal
        </div>
    )
}